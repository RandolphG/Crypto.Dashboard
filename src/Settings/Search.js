import React from "react";
import { SearchBar, SearchInput } from "../styles/Layout";

const Search = () => {
  return (
    <SearchBar>
      <h2>search coins</h2>
      <SearchInput />
    </SearchBar>
  );
};

export default Search;
